server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    regester-with-eureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
